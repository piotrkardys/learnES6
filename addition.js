export function sumTwo(a, b) {			//there are two ways of exporting elements
	return a + b;
}

export function sumThree(a, b, c) {		//we can export each function
	return a + b + c;
}

//export {sumTwo, sumThree}				//or we can export it in that way